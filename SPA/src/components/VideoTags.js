import Card from 'react-bootstrap/Card';

export default () => {
    return (
        <Card className='tags-card'>
            <Card.Body>
                tags
            </Card.Body>
        </Card>
    );
};