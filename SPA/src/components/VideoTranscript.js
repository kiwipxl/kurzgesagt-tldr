import Card from 'react-bootstrap/Card';

export default () => {
    return (
        <Card className='transcript-card'>
            <Card.Body>
                transcript
            </Card.Body>
        </Card>
    );
};